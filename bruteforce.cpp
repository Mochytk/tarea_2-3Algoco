#include <bits/stdc++.h>
using namespace std;

void fastio(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.setf(ios::fixed);
    cout.precision(4);
}

typedef long long ll;

int main(){
    fastio();
    return 0;
}
